01) Create the page in the exercise-page-4 folder.
  a) Include a css reset
  b) Make sure your css is linked to your document
  c) Use the css to make all changes to the style of the page.

02) Create an html page with an ordered list of 20 list items. Use an ID to select the third item. Make the item red.
03) Create an html page with an ordered list of 20 list items. Use a class to select the third, fourth, and fifth list items. Style them red.
04) Create an html page with 2 anchor tags to different websites. Give one of the anchor tags the target attribute. Use an attribute selector to select the anchor tag with the target attribute and style it to have a border around it.
05) Create an html page with an anchor tag. Provide styling for this anchor tag including different colors for these css pseudo-class properties: link, visited, hover, active
06) Create a paragraph of text. Style the first letter of that paragraph and the first line to be different than the rest of the paragraph.
07) If you are using emmet for code completion, use the following to create html within the html body:
    p{$}+div>p{$$}*3+article>p>lorem^^^p{$$$}*3
   Once the html is created, select all p tags that are children of a div and make them red.
08) Use the emmet from 7 to create html. Then select all p tags that are immediate children of a div and make them red.
09) Use the emmet from 7 to create html. Then select all p tags that are siblings following a div and make them red.
10) Use the emmet from 7 to create html. Then select all p tags that are immediate siblings following a div and make them red.
